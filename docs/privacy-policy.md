# Privacy policy — HardTalkAI

**Last updated:** 10 September 2026

This is a short, honest policy for a **prototype**. It is not legal advice and it does not claim GDPR/CCPA certification, a Data Protection Officer, or a production-grade retention program. If something here is wrong for how you run the app, fix the app or fix this page before you ship.

HardTalkAI (“the app”) is a **career coaching practice tool** (Coach Heather / leadership lane). You pick a workplace scenario, send practice replies to an AI counterpart, and get scores on clarity, empathy, and assertiveness.

**Contact:** [hello@hardtalk.ai](mailto:hello@hardtalk.ai). If that inbox is not yet monitored, use the repo owner: [aashish2k2@gmail.com](mailto:aashish2k2@gmail.com).

---

## What we collect (when you practice)

The app does **not** create accounts. There is no sign-in, profile, or email capture in this build.

When you run a drill, the client sends to our coaching API (`POST /api/chat`):

- the **scenario id** you picked
- the **text you typed** (your practice reply)
- the **conversation history** for that round (your earlier replies and the counterpart’s lines)

We use that payload only to score the turn and generate the next counterpart line. Coaching scores and tips are computed on our server. The scenario list (`GET /api/scenarios`) is fetched so the catalog can render.

This prototype **does not write practice chats to a database**. The request is handled in memory. Ordinary web-server logs may still record IP address, time, and the URL path. Error logs may include a stack trace; they are not a chat archive.

### Optional OpenAI

If the operator sets `OPENAI_API_KEY` on the server, the counterpart’s reply is generated by OpenAI (default model `gpt-4o-mini` unless `OPENAI_MODEL` is set). In that case the **scenario prompt, history, and your latest message** are sent to OpenAI so it can stay in character. If no key is set, the server uses a built-in deterministic reply and **does not** call OpenAI.

Coaching scores/tips are **not** sent to OpenAI; they stay on our server.

OpenAI’s handling of that data is governed by [OpenAI’s privacy policy](https://openai.com/policies/privacy-policy). We do not control their retention.

## What we do not collect / do not do

- We **do not sell** personal information.
- We **do not** run ads, attribution SDKs, or analytics packages in this build.
- We **do not** ask for your name, email, phone, location, contacts, photos, or microphone (no voice in this lane).
- We **do not** take payments in this build. There is **no RevenueCat** (or other IAP) integration yet. A later free-tier gate is catalog-only (`free` on the raise drill); it is not a paywall.
- Practice messages are **not** posted publicly and are **not** shown to other users.
- Children: this is a workplace-coaching prototype aimed at adults. Do not use it if you are under 13. We do not knowingly collect data from children.

Android’s `INTERNET` permission is used to reach the coaching API. Debug builds may talk to a machine on your network over HTTP; a store build should point at HTTPS.

## Retention and deletion

Because this prototype does not store chats in a database, there is usually **nothing to delete** on our side after the request finishes. If you believe a log or a hosted environment kept something of yours, email [hello@hardtalk.ai](mailto:hello@hardtalk.ai) or [aashish2k2@gmail.com](mailto:aashish2k2@gmail.com) and we will look.

If OpenAI was enabled for that session, we cannot erase copies that may exist in their systems; contact them as well if needed.

## Changes

This is a stub for Play Console and in-app disclosure. We will update this file when the product persists data, adds accounts, payments, analytics, or a production host. The “last updated” date will change.

## Play Console URL

Paste a **public HTTPS** URL to this policy into Play Console (Data safety + store listing). Options once this file is on the default branch:

1. GitHub: `https://github.com/Ashishsingh009/HardTalkAI/blob/main/docs/privacy-policy.md`
2. This HTML: `docs/privacy-policy.html` (host via GitHub Pages or the FastAPI `/privacy` route on a public server)

GitHub blob URLs work as a stopgap. GitHub Pages or your own host is cleaner for review.
